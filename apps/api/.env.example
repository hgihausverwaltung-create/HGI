DATABASE_URL="postgresql://hgi:hgi_dev_password@localhost:5432/hgi_dev"
JWT_SECRET="change-me-in-production"
PORT=4000
# Comma-separated list; omit to use the built-in dev defaults (web + mobile web targets)
# CORS_ORIGIN="http://localhost:5173,http://localhost:8081"
